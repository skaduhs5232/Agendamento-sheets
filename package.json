{
  "name": "agendamento-psicologos",
  "version": "1.0.0",
  "description": "Web aplicação para marcação de consultas com psicólogos",
  "main": "index.html",
  "scripts": {
    "start": "node start-server.js",
    "config": "node apply-config.js",
    "serve": "http-server -p 8080 -o",
    "build": "echo 'Build concluído - projeto estático pronto'",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ."
  },
  "dependencies": {
    "typescript": "^5.6.2"
  },
  "devDependencies": {
        "gh-pages": "^6.3.0",
    "http-server": "^14.1.1"
  }
}
